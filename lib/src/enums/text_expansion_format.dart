/// An enum which describes the different text expansion options
enum TextExpansionFormat {
  /// append text at the end of the string, i.e. MyString abd
  append,

  /// repeats vowels, i.e. MyyStriing
  repeatVowels,
}
